# Django Settings
DJANGO_SECRET_KEY=django-insecure-test-secret-key-change-in-production
DJANGO_DEBUG=1

# Database Configuration (MySQL)
DB_ENGINE=django.db.backends.mysql
DB_NAME=crm_poc
DB_USER=root
DB_PASSWORD=root
DB_HOST=db
DB_PORT=3306

# CRM API Key for Drupal Integration
CRM_API_KEY=4e67bd0be3c363eda173bb895b0af754df3a2fd2

# CORS & API Settings
ALLOWED_HOSTS=*
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8080

# Email Configuration (optional)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True

# Logging
LOG_LEVEL=INFO

# Environment
ENVIRONMENT=development